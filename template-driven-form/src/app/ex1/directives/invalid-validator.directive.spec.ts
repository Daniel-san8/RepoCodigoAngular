import { InvalidValidatorDirective } from './invalid-validator.directive';

describe('InvalidValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
